# Секреты — НИКОГДА в git
.env
.env.*
!.env.example
*.token
*token*.txt
secrets/
.secrets/
credentials*
local.properties

# GPL-данные — НИКОГДА в публичный репо (CLAUDE.md §10, копилефт): база AI-артистов
# slopless грузится в рантайме отдельной поставкой, в APK попадает только локально.
slopless.json
**/assets/slopless.json

# Аудио/бинарные артефакты
*.enc
*.flac
*.mp4
*.part
*.apk
*.keystore

# Gradle / сборка
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android
*.iml
.idea/
captures/

# ОС
.DS_Store
Thumbs.db
